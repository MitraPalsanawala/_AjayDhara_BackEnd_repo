var express = require("express");
//const { authJwt } = require("../middlewares");
//router.get("/all", [authJwt.verifyToken], AuthController.all);
var router = express.Router();

module.exports = router;